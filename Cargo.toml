[package]
name = "pokespecrs"
version = "0.1.0"
edition = "2024"
description = "Generate Pokemon from the command-line!"

[dependencies]
clap = { version = "4.5.40", features = ["color", "derive"] }
colored = "3.0.0"
log = "0.4.27"
rand = "0.9.1"
